// Maps an X-Factor ability name to its icon image. Drop the image in
// src/assets/xfactors/, import it below, and add an entry — then set
// `xFactor: "<name>"` on a player in players.ts to use it. Add entries like:
// import sniperIcon from "@/assets/xfactors/sniper.png";
// export const xFactorIcons: Record<string, string> = {
//   Sniper: sniperIcon,
// };
export const xFactorIcons: Record<string, string> = {};
